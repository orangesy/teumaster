server {
        location ~ /teuthology/ {
                rewrite /teuthology/(.*)   /$1   break;
        	allow all;
        	autoindex on;
#        server_name *;
        	root /opt/archive;
        	default_type text/plain;
	}
}
